# LearnLink Backend Gateway

Express API gateway for auth/onboarding, Stripe webhook intake, and proxy routing to domain services.

## Run

```bash
npm install
npm run dev
```

Default port: `4000`.

